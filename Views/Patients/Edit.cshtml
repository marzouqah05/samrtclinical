@model WebApplication1.Models.Patient

@{
    var _isAr = CultureInfo.CurrentUICulture.TwoLetterISOLanguageName == "ar";
    string T(string ar, string en) => _isAr ? ar : en;
    ViewData["Title"] = T("تعديل بيانات المريض", "Edit Patient");
    var todayStr = DateTime.UtcNow.ToString("yyyy-MM-dd");
    var dobVal = Model?.DOB.ToString("yyyy-MM-dd");
}

<style>
    .patient-form-card .patient-input {
        background-color: #f8fafc;
        border: 1px solid #cbd5e1;
        color: #0f172a;
        border-radius: 0.5rem;
        padding: 0.65rem 0.85rem;
        font-size: 0.85rem;
        width: 100%;
        transition: border-color 0.15s ease, box-shadow 0.15s ease, background-color 0.15s ease;
    }
    .patient-form-card .patient-input::placeholder {
        color: #94a3b8;
    }
    .dark .patient-form-card .patient-input {
        background-color: rgba(30, 41, 59, 0.7);
        border: 1px solid #334155;
        color: #f8fafc;
    }
    .dark .patient-form-card .patient-input::placeholder {
        color: #64748b;
    }
    .patient-form-card .patient-input:focus {
        outline: none;
        border-color: #10b981;
        box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.2);
        background-color: #ffffff;
    }
    .dark .patient-form-card .patient-input:focus {
        background-color: #1e293b;
        border-color: #10b981;
        box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.25);
        color: #ffffff;
    }
    .patient-form-card .field-validation-error,
    .patient-form-card span.field-validation-error {
        display: block !important;
        margin-top: 0.35rem !important;
        font-size: 0.75rem !important;
        color: #f43f5e !important;
        width: 100% !important;
    }
    .patient-form-card .field-validation-valid {
        display: none !important;
    }
</style>

<div dir="@(_isAr ? "rtl" : "ltr")" class="py-6 px-4">
    <!-- Dedicated Patient Edit Page Container -->
    <div class="patient-form-card bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 rounded-2xl p-6 sm:p-8 shadow-sm max-w-4xl mx-auto">
        
        <!-- Header -->
        <div class="flex items-center justify-between gap-3.5 pb-6 border-b border-slate-200 dark:border-slate-800 mb-6 flex-wrap">
            <div class="flex items-center gap-3.5">
                <div class="w-12 h-12 rounded-xl bg-emerald-500/10 text-emerald-500 dark:text-emerald-400 flex items-center justify-center text-xl shrink-0">
                    <i class="fa-solid fa-user-pen"></i>
                </div>
                <div>
                    <h2 class="text-lg sm:text-xl font-bold text-slate-900 dark:text-white m-0">
                        @T("تعديل بيانات المريض", "Edit Patient Profile")
                    </h2>
                    <p class="text-xs sm:text-sm text-slate-500 dark:text-slate-300 font-normal m-0 mt-0.5">
                        @T("تحديث البيانات الأساسية ومعلومات الاتصال والسجل الطبي للمريض.", "Update baseline demographics, contact information, and medical profile.")
                    </p>
                </div>
            </div>
            @if (!string.IsNullOrEmpty(Model?.PatientNumber))
            {
                <span class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-semibold bg-emerald-500/10 text-emerald-600 dark:text-emerald-400 border border-emerald-500/20">
                    <i class="fa-solid fa-folder-open text-[11px]"></i>
                    @T("رقم الملف:", "Record #:") #@Model.PatientNumber
                </span>
            }
        </div>

        <form asp-action="Edit" method="post" id="patientEditForm" novalidate>
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="PatientId" />
            <input type="hidden" asp-for="PatientNumber" />

            @if (!ViewData.ModelState.IsValid && ViewData.ModelState[string.Empty]?.Errors.Count > 0)
            {
                <div asp-validation-summary="ModelOnly" class="p-3.5 rounded-xl bg-rose-500/10 border border-rose-500/20 text-rose-600 dark:text-rose-400 text-xs mb-5"></div>
            }

            <!-- ═══════════════════════════════════════════════════════════════════ -->
            <!-- SECTION 1: Personal & Contact Information -->
            <!-- ═══════════════════════════════════════════════════════════════════ -->
            <div class="mb-7">
                <div class="flex items-center gap-2 mb-4">
                    <span class="inline-flex items-center justify-center w-6 h-6 rounded-lg bg-emerald-500/10 text-emerald-500 dark:text-emerald-400 text-xs">
                        <i class="fa-solid fa-id-card-clip"></i>
                    </span>
                    <h3 class="text-xs font-bold uppercase tracking-wider text-slate-700 dark:text-slate-200 m-0">
                        @T("البيانات الشخصية ومعلومات الاتصال", "Personal & Contact Information")
                    </h3>
                    <div class="flex-1 border-t border-slate-200 dark:border-slate-800 ms-2"></div>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <!-- Row 1: Full Name & National ID -->
                    <div>
                        <label asp-for="PatientName" class="block text-xs font-medium text-slate-700 dark:text-slate-200 mb-1.5">
                            @T("الاسم الكامل", "Full Name") <span class="text-rose-500">*</span>
                        </label>
                        <input asp-for="PatientName" 
                               autocomplete="off"
                               class="patient-input" 
                               placeholder="@T("مثال: أحمد محمد علي", "e.g. Ahmed Ali")" />
                        <span asp-validation-for="PatientName" class="text-rose-500 text-[11px] mt-1 block"></span>
                    </div>

                    <div>
                        <label asp-for="NationalId" class="block text-xs font-medium text-slate-700 dark:text-slate-200 mb-1.5">
                            @T("الرقم الوطني / الهوية", "National ID / Iqama") <span class="text-rose-500">*</span>
                        </label>
                        <input asp-for="NationalId" 
                               autocomplete="off"
                               class="patient-input" 
                               placeholder="@T("أدخل رقم الهوية أو الرقم الوطني", "Enter National ID number")" 
                               maxlength="50" />
                        <span asp-validation-for="NationalId" class="text-rose-500 text-[11px] mt-1 block"></span>
                    </div>

                    <!-- Row 2: Date of Birth & Phone Number -->
                    <div>
                        <label asp-for="DOB" class="block text-xs font-medium text-slate-700 dark:text-slate-200 mb-1.5">
                            @T("تاريخ الميلاد", "Date of Birth") <span class="text-rose-500">*</span>
                        </label>
                        <input asp-for="DOB" 
                               type="date" 
                               value="@dobVal"
                               max="@todayStr" 
                               class="patient-input" />
                        <span asp-validation-for="DOB" class="text-rose-500 text-[11px] mt-1 block"></span>
                    </div>

                    <div>
                        <label asp-for="PhoneNumber" class="block text-xs font-medium text-slate-700 dark:text-slate-200 mb-1.5">
                            @T("رقم الهاتف", "Phone Number") <span class="text-rose-500">*</span>
                        </label>
                        <input asp-for="PhoneNumber" 
                               autocomplete="off"
                               class="patient-input" 
                               placeholder="079xxxxxxx / +962xxxxxxx" />
                        <span asp-validation-for="PhoneNumber" class="text-rose-500 text-[11px] mt-1 block"></span>
                    </div>
                </div>
            </div>

            <!-- ═══════════════════════════════════════════════════════════════════ -->
            <!-- SECTION 2: Clinical & Medical Background -->
            <!-- ═══════════════════════════════════════════════════════════════════ -->
            <div>
                <div class="flex items-center gap-2 mb-4">
                    <span class="inline-flex items-center justify-center w-6 h-6 rounded-lg bg-emerald-500/10 text-emerald-500 dark:text-emerald-400 text-xs">
                        <i class="fa-solid fa-heart-pulse"></i>
                    </span>
                    <h3 class="text-xs font-bold uppercase tracking-wider text-slate-700 dark:text-slate-200 m-0">
                        @T("الخلفية الطبية والسريرية", "Clinical & Medical Background")
                    </h3>
                    <div class="flex-1 border-t border-slate-200 dark:border-slate-800 ms-2"></div>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <!-- Row 3: Gender & Blood Type -->
                    <div>
                        <label asp-for="Gender" class="block text-xs font-medium text-slate-700 dark:text-slate-200 mb-1.5">
                            @T("الجنس", "Gender")
                        </label>
                        <select asp-for="Gender" class="patient-input">
                            <option value="">@T("-- اختر الجنس --", "-- Select Gender --")</option>
                            <option value="Male">@T("ذكر", "Male")</option>
                            <option value="Female">@T("أنثى", "Female")</option>
                        </select>
                        <span asp-validation-for="Gender" class="text-rose-500 text-[11px] mt-1 block"></span>
                    </div>

                    <div>
                        <label asp-for="BloodType" class="block text-xs font-medium text-slate-700 dark:text-slate-200 mb-1.5">
                            @T("فصيلة الدم", "Blood Type")
                        </label>
                        <select asp-for="BloodType" class="patient-input">
                            <option value="">@T("-- اختر فصيلة الدم --", "-- Select Blood Type --")</option>
                            <option value="A+">A+</option>
                            <option value="A-">A-</option>
                            <option value="B+">B+</option>
                            <option value="B-">B-</option>
                            <option value="AB+">AB+</option>
                            <option value="AB-">AB-</option>
                            <option value="O+">O+</option>
                            <option value="O-">O-</option>
                        </select>
                        <span asp-validation-for="BloodType" class="text-rose-500 text-[11px] mt-1 block"></span>
                    </div>

                    <!-- Row 4: Allergies & Chronic Diseases -->
                    <div>
                        <label asp-for="Allergies" class="block text-xs font-medium text-slate-700 dark:text-slate-200 mb-1.5">
                            @T("الحساسية الدوائية / الغذائية", "Allergies")
                        </label>
                        <input asp-for="Allergies" 
                               autocomplete="off"
                               class="patient-input" 
                               placeholder="@T("مثال: حساسية البنسلين، أسبيرين، لا يوجد", "e.g. Penicillin, Aspirin, None")" />
                        <span asp-validation-for="Allergies" class="text-rose-500 text-[11px] mt-1 block"></span>
                    </div>

                    <div>
                        <label asp-for="ChronicDiseases" class="block text-xs font-medium text-slate-700 dark:text-slate-200 mb-1.5">
                            @T("الأمراض المزمنة", "Chronic Diseases")
                        </label>
                        <input asp-for="ChronicDiseases" 
                               autocomplete="off"
                               class="patient-input" 
                               placeholder="@T("مثال: السكري، ضغط الدم، الربو", "e.g. Diabetes, Hypertension, Asthma")" />
                        <span asp-validation-for="ChronicDiseases" class="text-rose-500 text-[11px] mt-1 block"></span>
                    </div>

                    <!-- Row 5: Clinical Notes (Full width textarea) -->
                    <div class="col-span-1 md:col-span-2">
                        <label asp-for="Notes" class="block text-xs font-medium text-slate-700 dark:text-slate-200 mb-1.5">
                            @T("ملاحظات سريرية وإدارية", "Clinical & Administrative Notes")
                        </label>
                        <textarea asp-for="Notes" 
                                  rows="3" 
                                  class="patient-input resize-y" 
                                  placeholder="@T("أضف أي ملاحظات طبية خاصة، سوابق جراحية، أو توجيهات للطاقم الطبي...", "Add any clinical remarks, surgical history, or staff instructions...")"></textarea>
                        <span asp-validation-for="Notes" class="text-rose-500 text-[11px] mt-1 block"></span>
                    </div>
                </div>
            </div>

            <!-- ═══════════════════════════════════════════════════════════════════ -->
            <!-- ACTION BUTTONS: Vibrant Hierarchy & Dedicated Styling -->
            <!-- ═══════════════════════════════════════════════════════════════════ -->
            <div class="flex items-center justify-end gap-3 pt-5 border-t border-slate-200 dark:border-slate-800 mt-6">
                <a asp-action="Index"
                   class="border border-slate-600 hover:bg-slate-800 text-slate-300 px-5 py-2.5 rounded-lg transition flex items-center gap-1.5 text-decoration-none text-xs font-semibold">
                    <i class="fa-solid fa-xmark text-xs"></i>
                    <span>@T("إلغاء", "Cancel")</span>
                </a>
                <button type="submit"
                        class="bg-emerald-600 hover:bg-emerald-500 text-white font-semibold px-6 py-2.5 rounded-lg shadow-lg shadow-emerald-900/30 transition flex items-center gap-2 border-0 cursor-pointer text-xs">
                    <i class="fa-solid fa-floppy-disk text-xs"></i>
                    <span>@T("تحديث بيانات المريض", "Save Changes")</span>
                </button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}